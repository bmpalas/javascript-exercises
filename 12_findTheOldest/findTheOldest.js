const findTheOldest = function() {

const a = []; // creat the array
people.forEach(function(obj){ //use Array.forEach()
    a.push(obj.yearOfBirth); // for each element, use Array.push() to push the 
    //element.title) 
})
return a;
};

// Do not edit below this line
module.exports = findTheOldest;
